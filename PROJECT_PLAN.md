이 프로젝트는 단순히 골프공을 찾는 프로그램이 아니라 다음 흐름을 가진 퍼팅 궤적 기록 프로그램입니다.
카메라 영상
→ 매트 영역 보정
→ 공의 시작 위치 등록
→ 공 움직임 감지
→ 프레임마다 중심 좌표 저장
→ 공 정지 또는 화면 이탈 판단
→ 아래쪽 결과 화면에 전체 궤적 표시
→ 결과 저장
1. 추천 화면 구성
위쪽: 실시간 카메라 화면
실제 카메라 영상을 그대로 보여줍니다.
표시할 정보:
매트 검출 영역
현재 공의 윤곽선
공 중심점
현재 상태
FPS
현재 좌표
검출 신뢰 상태
예:
상태: 시작 위치 대기
상태: 추적 준비
상태: 공 이동 중
상태: 추적 완료
아래쪽: 궤적 결과 화면
카메라 원본을 그대로 복사하기보다는, 캘리브레이션된 매트를 위에서 내려다본 평면으로 표시하는 것이 좋습니다.
┌──────────────────────────┐
│ 시작점 ●                 │
│       ·                  │
│         ·                │
│            ·             │
│               ● 정지점   │
└──────────────────────────┘
이 화면에는 다음 정보를 표시할 수 있습니다.
출발 위치
이동 경로
최종 정지 위치
총 이동 거리
이동 시간
평균 속도
출발 방향
좌우 편차
초기 개발에서는 우선 시작점, 경로, 마지막 점만 구현하면 됩니다.
2. 추천 버튼과 메뉴
사용자님이 생각하신 버튼은 모두 필요합니다.
반드시 필요한 버튼
① 캘리브레이션
카메라 영상에서 매트의 네 모서리를 지정합니다.
좌측 상단
우측 상단
우측 하단
좌측 하단
이 네 점을 이용해 기울어진 매트를 평면 좌표로 바꿉니다.
② 공 위치 초기화
매트 위에 공을 올려놓고 누릅니다.
이때 검출된 공 중심을:
시작점
으로 저장합니다.
버튼 이름은 다음처럼 표현하면 이해하기 쉽습니다.
시작 위치 등록
③ 추적 준비
공이 움직이기 전 프로그램을 대기 상태로 만듭니다.
대기
→ 공 이동 감지
→ 자동 추적 시작
버튼 이름 추천:
추적 준비
④ 추적 중지
오검출이나 실험 중 문제가 생겼을 때 수동으로 중지합니다.
⑤ 궤적 초기화
이전 퍼팅 경로만 지웁니다.
캘리브레이션 설정까지 지우면 안 됩니다.
⑥ 전체 초기화
공 시작점, 궤적, 현재 상태를 모두 초기화합니다.
⑦ 저장
저장 대상:
실행 날짜와 시간
시작 좌표
이동 좌표 전체
최종 좌표
이동 시간
스크린샷
설정값
초기에는 JSON이나 CSV로 저장하면 충분합니다.
3. 추가로 추천하는 기능
설정 불러오기
저장한 캘리브레이션과 HSV 설정을 다음 실행 때 다시 불러옵니다.
검출 설정
별도의 설정 창에서 다음을 조절합니다.
HSV 범위
최소·최대 면적
최소 원형도
최소·최대 반지름
정지 판정 시간
화면 이탈 판정 시간
매트 영역 표시
카메라 화면에서 실제 추적 대상 영역을 선으로 보여줍니다.
매트 밖의 사람, 신발, 전원장치 등은 검출 대상에서 제외해야 합니다.
영상 녹화
실험 실패 원인을 나중에 확인하기 위해 원본 영상을 저장할 수 있으면 좋습니다.
하지만 첫 버전에서는 나중에 구현해도 됩니다.
프로필
사진을 보면 실습 환경에서는 흰색 공을 사용하고, 실제 매트 사진에는 분홍색 공이 있습니다.
따라서 나중에는 다음과 같은 프로필을 둘 수 있습니다.
연습 환경 - 흰색 공
실제 매트 - 분홍색 공
각 프로필에 HSV와 면적 기준을 별도로 저장합니다.
4. 프로그램 상태 설계
버튼만 추가하는 것보다 프로그램의 상태를 먼저 정해야 합니다.
추천 상태는 다섯 가지입니다.
IDLE
→ 대기 상태

CALIBRATING
→ 매트 네 모서리 지정 중

READY
→ 시작 공 위치 등록 완료

TRACKING
→ 공 이동 좌표 수집 중

FINISHED
→ 공이 멈추거나 사라져 추적 완료
흐름은 다음과 같습니다.
프로그램 시작
→ 캘리브레이션
→ 공 시작 위치 등록
→ 추적 준비
→ 공 움직임 감지
→ 추적 시작
→ 공 정지 또는 이탈
→ 추적 종료
→ 아래 화면에 결과 표시
5. 공이 멈춘 것은 어떻게 판단하나요?
공의 현재 중심과 이전 중심의 거리를 비교합니다.
이동 거리가 일정 값보다 작음
+
일정 시간 동안 계속 유지됨
=
공이 멈춤
예:
프레임 간 이동량이 2픽셀 미만
그리고
0.5초 이상 계속 유지
→ 정지 판정
단 한 프레임만 움직이지 않았다고 멈춘 것으로 판단하면 안 됩니다. 검출 좌표는 조금씩 흔들릴 수 있기 때문입니다.
화면에서 사라진 경우도 별도로 처리합니다.
공 검출 실패가 0.3초 이상 지속
→ 화면 이탈로 추적 종료
정확한 시간은 실제 실험을 통해 조정해야 합니다.
6. 가장 중요한 개발 원칙
전체 기능을 한 번에 Codex CLI에 구현시키면 코드가 복잡해지고 오류 원인을 찾기 어렵습니다.
다음 순서로 나누어 개발하는 것이 좋습니다.
1단계: PySide6 UI 뼈대
2단계: 카메라 화면 연결
3단계: 기존 골프공 검출 코드 연결
4단계: 프로그램 상태 관리
5단계: 시작 위치 등록
6단계: 이동 좌표 저장
7단계: 정지·이탈 판정
8단계: 아래 결과 뷰에 궤적 그리기
9단계: 매트 캘리브레이션
10단계: 실제 좌표 변환
11단계: 설정 저장·불러오기
12단계: 결과 저장